# portfolio - Pendientes

## Pendientes

- Sync: cuando se publique un artículo nuevo en blog-articles, sincronizarlo acá (HTML + media) y pushear.
- UI (foundations, esfuerzo S ~2h, plegar a la próxima edición de estilos — NO sesión dedicada): extraer el `<style>` embebido de `resume.html` a `styles.css` y centralizar spacing/typo en CSS vars (index ya usa vars para color; resume los duplica hardcodeados). Opcional: stylelint-declaration-strict-value. Assessment 2026-06-27.
